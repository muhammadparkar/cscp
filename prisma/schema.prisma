datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model PharmacyData {
  id               Int      @id @default(autoincrement())
  name             String
  gender           String
  dosageNameCipher String
  ageCipher        String
  dosageCipher     String
  priceCipher      String
}
